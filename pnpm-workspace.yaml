# Build scripts allowed to run during install (pnpm denies them by default and
# errors on un-approved ones). Trusted native packages that need their
# postinstall: sharp (next/image), esbuild (vite/vitest), unrs-resolver (eslint).
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
